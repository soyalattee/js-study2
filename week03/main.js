import { question } from "readline-sync";

const yellowColor = { name: "elie", age: 20, gsn: "141414", money: 300000 };

console.log(users.money);

users.money = 400000;

console.log(users.money);
users.name = "soyeon";
users.age = 25;
console.log(users + `입니다`); // 문자랑 + 객체(변수)
console.log(` ${users} 입니다`);

 a e u i o 
 
 if ( str.charAt(0) == 'a' || str.charAt(0) == 'e' ){

 }
 else {

 }